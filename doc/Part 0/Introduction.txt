/**

\mainpage Introduction

\image html SkirtLogoSmaller.png
\image latex SkirtLogoSmaller.png

<i> © Astronomical Observatory, Ghent University. </i>

SKIRT is a state-of-the-art software code for simulating continuum radiation transfer in dusty astrophysical systems,
such as galaxies and accretion disks. SKIRT employs the Monte Carlo technique to emulate the relevant physical
processes including scattering, absorption and emission by the dust. The code features a wealth of built-in geometries,
light source spectra, dust characterizations, dust grids, and detectors, in addition to various mechanisms for
importing models generated by hydrodynamical simulations. The configuration for a particular simulation is defined
at run-time through a user-friendly (although non-graphical) interface suitable for both occasional and power users.

Specifically, SKIRT:
 - Offers full treatment of multiple anisotropic scattering, absorption and (re-)emission by interstellar dust.
 - Computes the temperature distribution of the dust and the thermal dust re-emission self-consistently,
   even for larger optical depths.
 - Handles any combination of dust mixtures, each with their own spatial distributions, and calculates
   the temperature of different grain size bins individually.
 - Takes into account the stochastic heating of small grains, optionally using an efficient library approach.
 - Handles any 3D geometry without limitation.
 - Includes many built-in components to specify the distribution of stars and dust, and allows importing
   the results of a hydrodynamic simulation.
 - Includes a suite of instruments for measuring the radiation field, mimicking real astronomical
   instrumentation such as imaging and 3D spectroscopic devices.
 - Allows specifying simulation parameters in an interactive console Q&A session and through
   XML-based parameter files.
 - Operates efficiently in both memory consumption and CPU time, using shared memory parallelization.

The SKIRT source tree includes FitSKIRT, an extension of the SKIRT code that automatically fits a 3D model to
observed images of a dusty galaxy. FitSKIRT matches the output frames of SKIRT radiative transfer simulations
to the observed images, searching the model parameter space through a genetic algorithm. For each new individual
constructed by the genetic procedure, FitSKIRT performs a SKIRT simulation using the parameters defined by the
individual's genes. The resulting simulated images are compared with the observed target images, and the result
is fed back into the genetic algorithm as the quality measure for the individual.

SKIRT is completely written in C++ and has been designed with well-defined programming interfaces between components.
Adding a new feature is usually as simple as adding another class; the user interface automatically adjusts to allow
configuring the new options.

This manual is structured as follows:
  - Part 0: Introduction (this page) and \ref LegalInfo.
  - Part I: Guides -- provides overall guidance and tutorials.
    - The \ref InstallationGuide describes how to install SKIRT on your computer.
    - The \ref UsersGuide explains how to run a SKIRT simulation and what output to expect.
    - The \ref FSUsersGuide explains how to prepare for and run a FitSKIRT simulation and what output to expect.
    - The \ref DevelopersGuide offers guidelines on how to extend or adjust the SKIRT code.
  - Part II: Concepts -- offers extra information on selected topics.
    - \ref DustProp describes how SKIRT deals with optical and calorimetric dust properties.
    - \ref PerInstr provides details on SKIRT's perspective instrument, used mostly for making movies.
    - \ref Cfitsio introduces the library included with SKIRT to read and write FITS files.
    - \ref voro introduces the library included with SKIRT to construct Voronoi tesselations.
    - \ref GAlib introduces the library included with FitSKIRT to search the model parameter space.
  - Part III: Reference -- documents every class, namespace and function.

If you are browsing an HTML version of this documentation:
  - select the "Related Pages" tab at the top of the help window to access the information in Part I and II;
  - select the "Classes" tab at the top of the help window to access the reference material.

The SKIRT web site can be found at <a href="http://www.skirt.ugent.be">www.skirt.ugent.be</a>.
Specifically:
 - The SKIRT documentation is available online in html format at
   <a href="http://www.skirt.ugent.be/skirt">www.skirt.ugent.be/skirt</a>.
 - It can be downloaded as a PDF file from
   <a href="http://www.skirt.ugent.be/downloads.html">www.skirt.ugent.be/downloads</a>.

*/
